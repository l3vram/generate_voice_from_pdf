version: '3.8'

services:
  pdf-to-audiobook:
    build: .
    volumes:
      - ./input:/app/input
      - ./output:/app/output
    environment:
      - PYTHONUNBUFFERED=1
    working_dir: /app
    tty: true
    stdin_open: true